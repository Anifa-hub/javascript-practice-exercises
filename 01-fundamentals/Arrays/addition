let numbers = [2,3,5]
let sum = 0;
for ( let i=0; i<numbers.length; i++) {
   sum = sum + numbers[i] }
   console.log(sum)
   // you want multiply
   let num = [2,2]
   let product = 1
   for ( let i= 0; i<num.length; i++){
    product = product * num[i]
    
   }
   console.log(product)